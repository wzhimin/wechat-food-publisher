# 微信小程序后台管理系统 — 实现文档

> 最后更新：2026-04-22

## 一、系统架构

### 技术栈
- **后端**：Express.js + Sequelize ORM + MySQL
- **前端**：纯 HTML + 原生 JS + Bootstrap 5 CDN（无构建工具）
- **认证**：账号密码登录（非微信 OpenID），Session 存储

### 登录信息
- 默认管理员：`admin` / `wang123456`
- 登录后通过 `/api/admin/login` 获取 token，后续请求 `Authorization: Bearer <token>`

---

## 二、数据库扩展

### 已执行的迁移（admin-migration.sql）

| 表 | 新增字段 | 说明 |
|---|---------|------|
| `recipe_comments` | `status` ENUM('pending','approved','rejected','deleted') DEFAULT 'pending' | 评论状态（自动审核通过，管理员可删除/还原） |
| `recipes` | `status` ENUM('pending','approved','rejected') DEFAULT 'approved' | 菜谱审核状态 |
| `recipes` | `is_featured` TINYINT(1) DEFAULT 0 | 精选标志 |
| `feedbacks` | `admin_reply` TEXT | 管理员回复 |
| `feedbacks` | `handled_by` VARCHAR(64) | 处理人 |
| `feedbacks` | `handled_at` DATETIME | 处理时间 |

### 新增表

**reports（举报表）**
| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT AUTO_INCREMENT | 主键 |
| type | ENUM('recipe','comment') | 举报类型 |
| recipeId | INT | 关联菜谱 |
| commentId | INT | 关联评论 |
| reason | VARCHAR(100) | 举报原因 |
| description | TEXT | 详细描述 |
| reporterOpenid | VARCHAR(128) | 举报人 |
| status | ENUM('pending','handled','dismissed') | 处理状态 |
| handled_by | VARCHAR(64) | 处理人 |
| handled_at | DATETIME | 处理时间 |

### Sequelize 同步模式
模型使用 `alter: 'safe'` 模式同步。**注意**：此模式下添加新字段时不能同时创建 indexes，否则部署失败。

---

## 三、API 端点一览

所有管理 API 以 `/api/admin` 开头，需在 Header 携带 `Authorization: Bearer <token>`。

### 认证
```
POST /api/admin/login          # 登录 → { token, admin: { username } }
```

### 数据统计
```
GET  /api/admin/stats          # 仪表盘数据（总菜谱/用户/评论/反馈）
GET  /api/admin/users/stats    # 用户统计（总数/今日/活跃7天/贡献者）
```

### 评论管理
```
GET   /api/admin/comments/list         # 评论列表 (?status=&page=&pageSize=)
POST  /api/admin/comments/delete       # 删除评论（软删除，status='deleted'，扣减 commentCount）
POST  /api/admin/comments/restore      # 还原评论（恢复为 approved，增加 commentCount）
```

### 菜谱管理
```
GET    /api/admin/recipes              # 菜谱列表 (?status=&page=&pageSize=&search=&source=)
PUT    /api/admin/recipes/:id          # 编辑菜谱
DELETE /api/admin/recipes/:id          # 删除菜谱
POST   /api/admin/recipes/feature/:id  # 切换精选状态
DELETE /api/admin/recipes/batch        # 批量删除 (body: { ids: [] })
```

### 用户管理
```
GET  /api/admin/users/list     # 用户列表 (?page=&pageSize=)
```
> **注意**：User 模型无 `last_login` 字段，统计活跃用户用 `updated_at` 替代。

### 反馈管理
```
GET    /api/admin/feedbacks           # 反馈列表 (?status=&page=&pageSize=)
POST   /api/admin/feedbacks/:id/reply # 回复反馈 (body: { reply: '' })
DELETE /api/admin/feedbacks/:id       # 删除反馈
```

### 举报管理
```
GET   /api/admin/reports              # 举报列表 (?status=&page=&pageSize=)
POST  /api/admin/reports/:id/handle   # 处理举报 (body: { status, note })
```

### 非菜谱清理
```
GET    /api/admin/non-dishes          # 疑似非菜谱列表
DELETE /api/admin/non-dishes          # 批量删除 (body: { ids: [] })
```
> **注意**：RecipeNote 无 `recipeId` 字段，批量删除时需排除该模型。

### 图片上传
```
POST /api/upload-image          # 上传图片 (body: { imageBase64: '' }) → { success, url }
```

---

## 四、前端页面

路径：`public/admin/`

| 页面 | 文件 | 功能 |
|------|------|------|
| 仪表盘 | index.html | 数据概览（菜谱/用户/评论/反馈统计） |
| 评论管理 | comments.html | 查看/删除/还原评论，按状态筛选（已通过/已删除/待审核/已拒绝/全部） |
| 菜谱管理 | recipes.html | 编辑/删除/设精选/批量删除，支持图片上传封面，支持来源筛选 |
| 系统菜谱 | system-recipes.html | 仅展示系统导入菜谱（authorOpenid='system'），可搜索、设精选 |
| 反馈管理 | feedbacks.html | 查看/回复/删除反馈 |
| 用户管理 | users.html | 用户列表（OpenID/昵称/头像/注册时间） |
| 非菜谱清理 | non-dishes.html | 批量检测和删除非菜谱条目 |
| 举报管理 | reports.html | 查看/处理举报（设为已处理/驳回） |

### 通用设计
- 所有页面共享 `admin.css` 和 `admin-auth.js`
- 侧边栏导航，统一登录态校验
- 编辑弹窗：菜谱编辑支持封面 URL 输入 + 本地图片上传（→ `/api/upload-image`）

---

## 五、小程序端配套修改

### 登录组件
- 新建 `components/login-modal/`（4文件），publish 页改用弹窗替代跳转登录

### 举报功能
- `recipe-detail` 页右下角 ⋯ 菜单添加「举报」按钮
- 举报 API：`POST /api/recipes/:id/report`

### 下拉刷新
- 已为 10 个列表页添加 `onPullDownRefresh`：index、collect、history、liked-recipes、my-recipes、search、season、fans、following、note

### 登录跳转修复
- profile 页：未登录时自动 `switchTab` 到 mine 页触发登录弹窗（之前直接跳 profile 会看到空表单）

---

## 六、内容审核策略

| 内容类型 | 策略 | 说明 |
|---------|------|------|
| 用户发布菜谱 | 自动通过 | `status='approved'`，依赖举报机制兜底 |
| 用户评论 | 自动通过 | 同上，管理员可在后台删除违规评论或还原误删（软删除 `status='deleted'`） |
| 图片 | img_sec_check | 发布时调用微信图片安全检测 |

### 图片安全检测
后端在菜谱发布/更新时，如有新上传图片，调用微信 `img_sec_check` 接口检测。违规图片拒绝发布。

### 评论管理说明
- 评论发布时自动审核通过（`status='approved'`），几乎无 pending 状态
- 后台评论管理页面默认展示「已通过」的评论
- 管理员可对违规评论执行**删除**（软删除，`status='deleted'`，用户端不可见）
- 误删的评论可**还原**（恢复为 `approved`，恢复 commentCount）
- 用户端评论列表只展示 `status='approved'` 的评论

---

## 七、运维脚本

| 脚本 | 路径 | 用途 |
|------|------|------|
| 封面补全 | `scripts/fill-recipe-covers.js` | 为无封面菜谱搜索 Pixabay 图片 |
| 封面失效检测 | `scripts/check-pixabay-covers.js` | 检测第三方封面（Pixabay/Pexels）是否失效，支持 `--fix` 自动修复 |
| 非菜谱检测 | `scripts/clean_non_dish.js` | 扫描疑似非菜谱条目（纯食材名等） |

---

## 八、已知问题与待办

1. ~~admin.js 引用 User.last_login~~ ✅ 已修复，改用 updated_at
2. ~~admin.js path is not defined~~ ✅ 已补 `const path = require('path')`
3. ~~recipes.html 封面编辑不支持图片上传~~ ✅ 已支持本地上传
4. ~~评论审核改为删除/还原~~ ✅ 已完成，软删除机制
5. ~~精选功能后台已有，但小程序端尚未展示精选菜谱入口~~ ✅ 小程序首页已展示精选角标（轮播/推荐/列表均有）
6. ~~Pixabay 代理链接失效~~ 需定期运行 `check-pixabay-covers.js` 检测（尚未自动化）
7. ~~非菜谱清理脚本连接配置~~ ✅ 已适配，可连接腾讯云 MySQL
8. ~~reports 表不存在~~ ✅ 已部署，Report 模型已创建
9. ~~users 表列名 openId~~ ✅ 已部署，openid 字段已统一
10. ~~feedbacks 表名不一致~~ ✅ 已部署，tableName 统一为 feedbacks，启动时自动迁移
